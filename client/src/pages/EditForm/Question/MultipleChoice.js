import React, { useState, useRef, useEffect } from "react";
import {
    Box,
    TextField,
    Typography,
} from "@mui/material";

function MultipleChoice({ reduced, editing, editProp }) {
    const contsx = {
        width: "100%",
    }

    return (
        <Box sx={contsx}>

        </Box>
    )
}

export default MultipleChoice;